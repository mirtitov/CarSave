# 🔧 Исправление проблемы входа в админ-панель

## Проблема:
Ошибка "Неверный email или пароль" при попытке входа.

## Возможные причины:
1. Администратор не создан в базе данных
2. Неправильный пароль в базе данных
3. Проблема с подключением к базе данных

## Решение:

### Вариант 1: Проверить и создать администратора через SQL в Supabase

1. Откройте Supabase Dashboard
2. Перейдите в **SQL Editor**
3. Выполните этот SQL:

```sql
-- Проверить, есть ли администратор
SELECT * FROM "Admin" WHERE email = 'admin@carsave.ru';

-- Если администратора нет, создать его
-- Пароль: admin123
INSERT INTO "Admin" (id, email, password, name, "createdAt", "updatedAt")
VALUES (
  gen_random_uuid()::text,
  'admin@carsave.ru',
  '$2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy',
  'Администратор',
  NOW(),
  NOW()
)
ON CONFLICT (email) DO UPDATE SET
  password = EXCLUDED.password,
  "updatedAt" = NOW();
```

### Вариант 2: Использовать скрипт проверки (если есть доступ к базе локально)

```bash
npm run check-admin
```

Этот скрипт:
- Проверит, есть ли администратор
- Создаст его, если нет
- Обновит пароль, если он неправильный

### Вариант 3: Проверить переменные окружения в Vercel

Убедитесь, что в Vercel настроены:
- `DATABASE_URL` - правильный Connection String из Supabase
- `NEXTAUTH_SECRET` - секретный ключ
- `NEXTAUTH_URL` - URL сайта (https://carsave-garant.vercel.app)

## После исправления:

1. Попробуйте войти снова:
   - Email: `admin@carsave.ru`
   - Password: `admin123`

2. Если всё ещё не работает, проверьте логи в Vercel:
   - **Deployments** → последний деплой → **Logs**
   - Ищите ошибки подключения к базе данных
