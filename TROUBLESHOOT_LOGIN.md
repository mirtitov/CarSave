# 🔧 Устранение проблемы входа в админ-панель

## Проблема:
Администратор создан в базе данных, но войти не получается.

## Шаги диагностики:

### 1. Проверьте пароль в базе данных

Выполните в Supabase SQL Editor:

```sql
-- Проверить текущий пароль
SELECT id, email, name, 
       LEFT(password, 20) as password_start,
       LENGTH(password) as password_length
FROM "Admin" 
WHERE email = 'admin@carsave.ru';
```

Пароль должен начинаться с `$2a$10$` и иметь длину 60 символов.

### 2. Обновите пароль (если нужно)

```sql
DO $$
DECLARE
  correct_password_hash TEXT := '$2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy';
BEGIN
  UPDATE "Admin" 
  SET 
    password = correct_password_hash,
    "updatedAt" = NOW()
  WHERE email = 'admin@carsave.ru';
  
  RAISE NOTICE 'Password updated';
END $$;
```

### 3. Проверьте переменные окружения в Vercel

В Vercel Dashboard → Settings → Environment Variables проверьте:

- ✅ `DATABASE_URL` - правильный Connection String из Supabase
- ✅ `NEXTAUTH_SECRET` - установлен
- ✅ `NEXTAUTH_URL` - `https://carsave-garant.vercel.app`

### 4. Проверьте логи в Vercel

1. Откройте Vercel Dashboard → **Deployments**
2. Выберите последний деплой
3. Перейдите в **Logs**
4. Попробуйте войти снова
5. Ищите ошибки:
   - "Can't reach database server"
   - "Connection timeout"
   - "Invalid credentials"
   - Любые ошибки Prisma

### 5. Проверьте подключение к базе данных

Проблема может быть в том, что:
- Connection String неправильный
- База данных недоступна из Vercel
- Нужно использовать Pooler Connection String вместо Direct

### 6. Попробуйте Pooler Connection String

В Supabase:
1. **Settings** → **Database**
2. Найдите **Connection String** → **Connection Pooling**
3. Скопируйте Connection String (должен быть на порту 6543, не 5432)
4. Обновите `DATABASE_URL` в Vercel
5. Сделайте Redeploy

### 7. Проверьте, что Prisma Client сгенерирован

В логах сборки должно быть:
```
Running "prisma generate"
```

Если нет, проверьте `package.json`:
```json
"build": "prisma generate && next build"
```

## Что проверить в логах:

После попытки входа проверьте логи на наличие:
- Ошибок подключения к базе данных
- Ошибок Prisma
- Ошибок NextAuth

## Быстрая проверка:

1. Выполните SQL для обновления пароля (см. выше)
2. Проверьте Connection String в Vercel
3. Попробуйте войти снова
4. Если не работает - пришлите логи из Vercel
